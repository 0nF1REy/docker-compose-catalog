# === Caminho para armazenar dados (opcional se não for usar fora do container)
DATA_FOLDER=/root/n8n

# === Domínio externo com nip.io (redireciona IPs para subdomínios automaticamente)
DOMAIN_NAME=142.93.245.175.nip.io
SUBDOMAIN=n8n

# === Autenticação básica para acessar o n8n (ALTERE PARA SUA SENHA REAL)
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=TroqueEssaSenha123!

# === Timezone da aplicação e do PostgreSQL
GENERIC_TIMEZONE=America/Sao_Paulo

# === E-mail para certificados SSL (caso use com Traefik ou Nginx com Certbot)
SSL_EMAIL=seu-email@dominio.com

# === Variáveis do banco de dados PostgreSQL
POSTGRES_DB=n8n
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=senhaFort3Aqui
